# Data Mode
# Options: mock | live
# - mock: Use generated mock data (default, safe for development)
# - live: Fetch from /public/data/ JSON artifacts (requires backend data)
VITE_DATA_MODE=mock

# Feature Flags (Priority 1 - DGP Core Features)
# Set to true to enable, false to hide (default: false until implemented)

# OCCTO Reserve Margin (badge/gauge showing system tightness)
VITE_FEAT_RESERVE=false

# JEPX Spot Prices (overlay on demand charts + dedicated /prices route)
VITE_FEAT_JEPX=false

# Settlement-lite (cost card with kWh × JPY/kWh calculation)
VITE_FEAT_SETTLEMENT=false

# Comparison View (Tokyo vs Kansai overlay on /comparison route)
VITE_FEAT_COMPARISON=false

# API Base URL (for Mode B: Serverless API - future)
# Not used in Mode A (static JSON files)
# VITE_API_BASE=http://localhost:8080/api
